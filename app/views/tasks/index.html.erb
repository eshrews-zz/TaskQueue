<h1>Listing tasks</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Lastreminderdelay</th>
    <th>Completed</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @tasks.each do |task| %>
  <tr>
    <td><%= task.name %></td>
    <td><%= task.lastActivationOffset %></td>
    <td><%= task.completed %></td>
    <td><%= task.updated_at %></td>
    <td><%= link_to 'Show', task %></td>
    <td><%= link_to 'Destroy', task, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />
<br />
<br />
<br />

<h1>Listing active tasks</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Lastreminderdelay</th>
    <th>Completed</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @tasks2.each do |task| %>
  <tr>
    <td><%= task.name %></td>
    <td><%= task.lastActivationOffset %></td>
    <td><%= task.completed %></td>
    <td><%= task.updated_at %></td>
    <td><%= task.nextActivationDate %></td>
    <td><%= link_to 'Show', task %></td>
    <td><%= link_to 'Destroy', task, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<%= link_to 'New Task', new_task_path %>
